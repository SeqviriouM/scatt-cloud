import React, {Component} from 'react';
import classNames from 'classnames';

import './TargetPage.scss';

export default class TargetPage extends Component {
    render() {
        return (
            <div className={classNames('target-page')}>
                Target
            </div>
        )
    }
}
